package com.webmobril.badoli.adapter;

/*
public class SpinnerAdapter extends BaseAdapter {

    Context context;
    String[] countryNames;
    String[] countrycode;
    LayoutInflater inflter;

    public SpinnerAdapter(Context applicationContext, String[] countrycode, String[] countryNames) {

        this.context = applicationContext;
        this.countrycode = countrycode;
        this.countryNames = countryNames;
        inflter = (LayoutInflater.from(applicationContext));
    }

    @Override
    public int getCount() {
        return countrycode.length;
    }

    @Override
    public Object getItem(int position) {
        return null;
    }

    @Override
    public long getItemId(int position) {
        return 0;
    }

  */
/*  @Override
    public View getDropDownView(int position, @Nullable View convertView, @NonNull ViewGroup parent) {
            LayoutInflater inflater = (LayoutInflater) context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);
        View view = inflater.inflate(R.layout.country_list, null);
        TextView sort_name = view.findViewById(R.id.country_sort_name);
        TextView phone_code = view.findViewById(R.id.country_phone_code);
        sort_name.setText(countryNames[position]);
        phone_code.setText(countrycode[position]);
        return view;
    }

    @Override
    public View getView(int position, View view, ViewGroup parent) {
        view = inflter.inflate(R.layout.country_list, null);
        TextView sort_name = (TextView) view.findViewById(R.id.country_sort_name);
        TextView phone_code = (TextView) view.findViewById(R.id.country_phone_code);
        sort_name.setText(countryNames[position]);
        phone_code.setText(countrycode[position]);
        return view;
    }*//*

}
*/
